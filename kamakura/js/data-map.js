/* ============================================================
 * えんそうのじゅんばん（2枚目・左上）
 * ------------------------------------------------------------
 * 手描きの図をそのまま起こしたフローチャート。
 * ここは**文字だけ**を持ちます。図の形と座標は render.js の
 * renderPlayMap に直書きしてあります（この図に固有で、
 * データにしても他で使いまわせないため）。
 *
 *   【カマクラ】  ［はじめ］⇓［ぶっつけ］⇓（ぐるぐる）⇓［あがり］
 *   - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
 *   【シチョウメ】    │←──────┐
 *                     ├→［タマ］┤   （この区間をくりかえす）
 *                     ⇓
 *                  ［きり］⇓［おわり］
 *
 * まっすぐな ↓ そのものが「地」。それを凡例（legend）で断ってある。
 *
 * ★ start を消す（または空にする）と、見出しだけの空の枠になります。
 * ============================================================ */
(function (YH) {
  'use strict';

  YH.PLAY_MAP = {
    title: 'えんそうのじゅんばん',

    /* 左に縦書きで置く、まとまりの名前 */
    kamakura: '【カマクラ】',
    shichoume: '【シチョウメ】',

    /* 四角の中の文字 */
    start: 'はじめ',
    buttsuke: 'ぶっつけ',
    agari: 'あがり',
    tama: 'タマ',
    kiri: 'きり',
    end: 'おわり',

    /* 凡例。矢印は図のほうで描くので、ここは矢印の右の文字だけ */
    legend: 'は　地'
  };
})(window.YH = window.YH || {});
