/* ============================================================
 * 演奏順（2枚目・左上）
 * ------------------------------------------------------------
 * ★★ この図は「仮」です。★★
 * くりかえしや戻りをまだ聞いていないので、
 *
 *     ［はじめ］⇓［昇殿］⇓［神田丸］⇓［おわり］
 *
 * の一直線にしてあります。手描きの図をもらったら、
 * render.js の renderPlayMap の座標ごと描き直してください。
 *
 * ここは**文字だけ**を持ちます。図の形と座標は render.js の
 * renderPlayMap に直書きしてあります（その図に固有で、
 * データにしても他で使いまわせないため）。
 *
 * ★ start を消す（または空にする）と、見出しだけの空の枠にもどります。
 * ============================================================ */
(function (YH) {
  'use strict';

  YH.PLAY_MAP = {
    title: '演奏順',

    /* 四角の中の文字。譜面のまとまりの名前（data-score-front.js の
       section.name）から【 】を外したもの */
    start: 'はじめ',
    shouden: '昇殿',
    kandamaru: '神田丸',
    end: 'おわり',

    /* 凡例。いまは矢印が1種類（そのまま先へ進む）だけなので出していない。
       枝分かれやくりかえしが入ったら、render.js で凡例の枠を描いて
       この文字を使ってください */
    legend: ''
  };
})(window.YH = window.YH || {});
